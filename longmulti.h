
/* longMulti:
 * Input: Two same sized integer arrays (a,b) which contain long numbers,
 *        and the size of the arrays (n).
 * 
 * Output: An array of size 2n which contains the long multiplication result of a and b.
 */
int* longMulti(int* a, int* b, int n);